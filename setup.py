from setuptools import setup

setup(
   name='fnl_pipe',
   version='1.0',
   description='An ACT x SDSS fnl pipeline',
   author='Alexander Roman',
   author_email='aroman@perimeterinstitute.ca',
   packages=['fnl_pipe', 'scripts'],
   install_requires=[],
)